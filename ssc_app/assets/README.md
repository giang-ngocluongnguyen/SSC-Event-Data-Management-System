Optional images can be added here:

- `ssc_logo.png` or `ssc_logo.jpg` for the sidebar logo.
- `ssc_background.png` or `ssc_background.jpg` for the app background.
- Uploaded event-card images are saved automatically in `assets/event_images/`.
